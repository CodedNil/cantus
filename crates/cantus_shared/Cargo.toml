[package]
name = "cantus_shared"
version = "0.1.0"
edition = "2024"

[dependencies]
bytemuck = { workspace = true, optional = true }
glam = { workspace = true }

[features]
default = []
cpu = ["bytemuck"]
gpu = []

[lints]
workspace = true
